const userInput = 1;
let result;
result = userInput + 18;
result = result - 1;
result = result * 3;
result = result / 2;

alert(result);
alert(userInput); // whether userInput is defined by 'let' or 'const' doesn't matter because it won't change either way