Classes
- allow us to build objects with a blueprint
- objects are instances of classes
- defined how objects look like, which properties and methods
  they have
- class-based creation is an alternative to using object literals { ... }
- classes can make the creation of multiple, similar objects much easier
- all classes have to be defined before the code that is used to execute them